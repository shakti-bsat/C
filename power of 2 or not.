#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num;
    scanf("%d",&num);
    printf("%s",((num &(num-1)) == 0)?"YES":"NO");
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
